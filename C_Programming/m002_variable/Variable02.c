//
// Created by AlgoKelvin on 1/17/22.
//

#include <stdio.h>

int main() {

    // Way 1: Initialize variable and set values
    int x, y, z; // Multiple initialize variable
    x = 10, y = 20, z = 30; // set values
    printf("x = %d \n y = %d \n z = %d \n", x, y, z);

    // Way 2: Multiple Define Values
    float a = 1.41, b = 2.3, c = 4.32;
    printf("a = %f \n b = %f\n c = %f", a, b, c);

    return 0;
}
