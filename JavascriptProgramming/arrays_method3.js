const z = ["Java", "Kotlin", "Dart", "PHP", "Javascript", "Python"]

// Show data start index 2 until end
document.getElementById('slice1').innerHTML = z.slice(2)

// Show 3 data start index 1 
document.getElementById('slice2').innerHTML = z.slice(1, 3)