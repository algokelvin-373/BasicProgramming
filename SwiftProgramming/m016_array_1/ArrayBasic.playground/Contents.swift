
import Cocoa

var arrayInt = [Int](); // Initialize Array at Int

// Array Count
print("Amount Data Array Int: \(arrayInt.count)")

// Array Empty
if arrayInt.isEmpty {
    print("This ArrayInt is empty")
}
